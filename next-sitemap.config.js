/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://logo.media/',
  generateRobotsTxt: true,
  generateIndexSitemap: false, // (optional)
  // ...other options
}

module.exports = config
